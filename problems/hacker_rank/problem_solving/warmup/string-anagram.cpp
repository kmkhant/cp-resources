#include <bits/stdc++.h>
using namespace std;
int main() {
    vector<string> dictionary = {"hack", "a", "rank", "khac", "ackh", "kran", "rankhacker", "a",
        "ab", "ba", "stairs", "raits"};
    vector<string> query = {"a", "nark", "bs", "hack", "stair"};
    
    sort(dictionary.begin(), dictionary.end());

    string currentQuery; vector<int> result; int count;

    return 0;
}
